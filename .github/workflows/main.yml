on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "befor listing files"
        run: ls

      - uses: actions/checkout@v3

      - name: "after listing files"
        run: ls
     
